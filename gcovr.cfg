# Settings for `npm run coverage` — see README.md.
root = .
filter = src/
filter = include/
exclude-throw-branches = yes
exclude-unreachable-branches = yes
print-summary = yes
txt = -
html-details = cmake-build-coverage/coverage.html
